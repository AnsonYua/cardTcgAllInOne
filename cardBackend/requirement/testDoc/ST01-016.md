  1. Deploy adds a shield card to hand

  - Setup: Player A has ST01-016 in hand and at least 1 energy. Player A has at least 1 shield card.
    currentPlayer = A, phase = MAIN_PHASE, notificationQueue includes CARD_DRAWN for playerId_1.
  - Action: Load a scenario where A has ST01-016 in hand and 1 shield in shieldArea, then play ST01-016 as a base.
  - Expect: One shield card is moved to A’s hand and the base is placed in base area.

  2. Main ability boosts linked units and rests the base

  - Setup: Player A has ST01-016 in play as a base (active), 1+ linked unit in play, and at least 1 active energy.
    currentPlayer = A, phase = MAIN_PHASE, notificationQueue includes CARD_DRAWN for playerId_1.
  - Action: Activate `activate_boost_link_units` via `activateCardAbility` on ST01-016.
  - Expect: ST01-016 becomes rested, and all friendly linked units gain AP+1 until end of turn.

  3. Main ability does nothing when no linked units exist

  - Setup: Player A has ST01-016 in play as a base (active), no linked units in play, and at least 1 active energy.
    currentPlayer = A, phase = MAIN_PHASE, notificationQueue includes CARD_DRAWN for playerId_1.
  - Action: Activate `activate_boost_link_units` via `activateCardAbility` on ST01-016.
  - Expect: ST01-016 becomes rested. No units gain AP (no linked units present).

  4. Cannot activate when base is already rested

  - Setup: Player A has ST01-016 in play as a base, and it is already rested. Player A has at least 1 active energy.
    currentPlayer = A, phase = MAIN_PHASE, notificationQueue includes CARD_DRAWN for playerId_1.
  - Action: Attempt `activateCardAbility` on ST01-016.
  - Expect: Action fails with an error indicating the base is already rested.

  5. Burst deploys the base from shield

  - Setup: Player A has ST01-016 in shield area. Player B can attack Player A’s shield.
    currentPlayer = B, phase = MAIN_PHASE, notificationQueue includes CARD_DRAWN for playerId_1.
  - Action: Player B calls the attack API to attack A’s shield (this transitions into attack flow and reveals the shield),
    then resolve burst via `confirmBurstChoice`.
  - Expect: ST01-016 is deployed to A’s base area and removed from shield.

  6. Boost effect ends when the base is destroyed

  - Setup: Player A has ST01-016 in play as a base (active) and a linked unit in slot1. Player B has an attacker that
    can destroy the base. currentPlayer = A, phase = MAIN_PHASE, notificationQueue includes CARD_DRAWN for playerId_1.
  - Action: Activate `activate_boost_link_units` via `activateCardAbility` on ST01-016.
  - Expect: Linked unit gains AP+1 until end of turn.
  - Action: End A’s turn, then Player B attacks the base to destroy it.
  - Expect: ST01-016 is moved to trash and the AP+1 effect is removed immediately from the linked unit.
